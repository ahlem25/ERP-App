spring:
  config:
    import: "configserver:http://localhost:8012"
  application:
    name: erp-category-service
  profiles:
    active: dev
  cloud:
    config:
      profile: ${spring.profiles.active}