spring:
  application:
    name: erp-service-discovery
  cloud:
    config:
      uri: http://localhost:8012
      profile: dev
  config:
    import: "configserver:http://localhost:8012"
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false